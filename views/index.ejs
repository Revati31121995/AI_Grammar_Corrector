<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AI Grammar Genius</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />

    <style>
      .icon-large {
        font-size: 3rem;
        margin-bottom: 1rem;
      }
      .result-box {
        white-space: pre-wrap;
      }
    </style>
  </head>

  <body class="bg-light">
    <div class="container mt-5">
      <div class="text-center mb-5">
        <i class="fas fa-robot icon-large text-primary"></i>
        <h1 class="display-4">AI Grammar Genius</h1>
        <p class="lead">
          Harness the power of artificial intelligence to perfect your writing
        </p>
      </div>

      <div class="row">
        <!-- Input -->
        <div class="col-md-6 mb-4">
          <div class="card">
            <div class="card-body">
              <h2 class="card-title">
                <i class="fas fa-keyboard me-2"></i>Input Your Text
              </h2>

              <form action="/correct" method="POST">
                <div class="mb-3">
                  <textarea
                    class="form-control"
                    name="text"
                    rows="6"
                    placeholder="Type or paste your text here..."
                    required
                  ><%= originalText || "" %></textarea>
                </div>

                <button type="submit" class="btn btn-primary">
                  <i class="fas fa-magic me-2"></i>Enhance with AI
                </button>
              </form>
            </div>
          </div>
        </div>

        <!-- Output -->
        <div class="col-md-6 mb-4">
          <div class="card">
            <div class="card-body">
              <h2 class="card-title">
                <i class="fas fa-check-circle me-2"></i>AI-Corrected Output
              </h2>

              <% if (corrected) { %>
                <div class="alert alert-success result-box mt-3">
                  <%= corrected %>
                </div>
              <% } else { %>
                <p class="text-muted mt-3">
                  Enter text and click “Enhance with AI”.
                </p>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
