<!DOCTYPE html>   <!--Tells browser that it uses HTML 5-->
<html lang="en">  <!--Root element. specifies english -->
  <head>
    <meta charset="UTF-8" /> <!-- Supports all characters -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0" /> <!-- Makes site mobile responsive -->
    <title>AI Grammar Genius</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />  <!-- Loads external bootstrap models -->

    <style>
      .icon-large {
        font-size: 3rem;
        margin-bottom: 1rem;
      }  
      .result-box {
        white-space: pre-wrap;
      }
    </style> <!-- icon-large makes icon larger -->
    <!-- white-space: pre-wrap: it preserves newlines from AI output. AI provides
     output without line breaks. Without it everything becomes one line -->
  </head>

  <body class="bg-light"> <!-- Bootstrap class for light background -->
    <div class="container mt-5"> <!-- Center contents add margin top -->
      <div class="text-center mb-5">
        <i class="fas fa-robot icon-large text-primary"></i>
        <h1 class="display-4">AI Grammar Genius</h1>
        <p class="lead">
          Harness the power of artificial intelligence to perfect your writing
        </p>
      </div>

      <div class="row">
        <!-- Input -->
        <div class="col-md-6 mb-4">
          <div class="card">
            <div class="card-body">
              <h2 class="card-title">
                <i class="fas fa-keyboard me-2"></i>Input Your Text
              </h2>

              <form action="/correct" method="POST"> <!-- Sends user text to backend route /correct by post method -->
                <div class="mb-3">
                  <textarea
                    class="form-control"
                    name="text"
                    rows="6"
                    placeholder="Type or paste your text here..."
                    required
                  ><%= originalText || "" %></textarea> <!-- <%= %>: Output value which comes from backend -->
                  <!-- original Text has been come from backend-->
                   <!--Why needed?
                   Keeps user input after submit and without this text area clears every time -->
                </div>

                <button type="submit" class="btn btn-primary">
                  <i class="fas fa-magic me-2"></i>Enhance with AI
                </button>
              </form>
            </div>
          </div>
        </div>

        <!-- Output -->
        <div class="col-md-6 mb-4">
          <div class="card">
            <div class="card-body">
              <h2 class="card-title">
                <i class="fas fa-check-circle me-2"></i>AI-Corrected Output
              </h2>

              <% if (corrected) { %>
                <div class="alert alert-success result-box mt-3">
                  <%= corrected %>
                </div>
              <% } else { %>
                <p class="text-muted mt-3">
                  Enter text and click “Enhance with AI”.
                </p>
              <% } %>
              <!-- <% %> Java script logic 
                   <%= %> Print value 
                   Displays AI corrected text-->
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
