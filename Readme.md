Browser
  ↓
HTML Form (index.ejs)
  ↓ POST /correct
Express Server (app.js)
  ↓
OpenAI API (gpt-4o-mini)
  ↓
Corrected Text
  ↓
Rendered back into EJS


index.ejs → Frontend (UI + templating)

app.js → Backend (server + AI integration)